name: Keep Render Awake

on:
  schedule:
    - cron: "*/5 * * * *"   # 5分おき（UTC）
  workflow_dispatch:        # 手動実行ボタンも残す

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      # - name: Curl SearxNG (robust)
      #   run: |
      #     set -euo pipefail
      #     curl -fsS -4 --location \
      #       --connect-timeout 5 \
      #       --max-time 20 \
      #       --retry 4 --retry-all-errors --retry-delay 2 --retry-max-time 40 \
      #       -A "keepalive-bot/1.0" \
      #       "https://zofumixng.onrender.com/favicon.ico" >/dev/null
      #     curl -fsS -4 --location \
      #       --connect-timeout 5 \
      #       --max-time 20 \
      #       --retry 4 --retry-all-errors --retry-delay 2 --retry-max-time 40 \
      #       -A "keepalive-bot/1.0" \
      #       "https://zofumixng.onrender.com/opensearch.xml" >/dev/null

      - name: Curl Personal Search (root)
        run: |
          set -euo pipefail
          curl -fsS -4 --location \
            --connect-timeout 5 \
            --max-time 20 \
            --retry 4 --retry-all-errors --retry-delay 2 --retry-max-time 40 \
            -A "keepalive-bot/1.0" \
            "https://zofusai.onrender.com/" >/dev/null

      - name: Curl Personal Search (thread_search)
        run: |
          set -euo pipefail
          curl -fsS -4 --location \
            --connect-timeout 5 \
            --max-time 20 \
            --retry 4 --retry-all-errors --retry-delay 2 --retry-max-time 40 \
            -A "keepalive-bot/1.0" \
            "https://zofusai.onrender.com/thread_search" >/dev/null
